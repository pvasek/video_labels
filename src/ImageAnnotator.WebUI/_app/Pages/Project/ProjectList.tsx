import React = require("react");
import { observer } from "mobx-react";

@observer
export class ProjectList extends React.Component<any, {}> {
    render() {
        return <div>
            PROJECT LIST
        </div>
    }
}